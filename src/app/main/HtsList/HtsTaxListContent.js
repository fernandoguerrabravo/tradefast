import { memo } from 'react';
import FullWidthGrid from './HtsTaxListapp';

const HtsTaxListContent = () => {
	return (
		<>
			<FullWidthGrid />
		</>
	);
};

export default memo(HtsTaxListContent);
