/* eslint-disable no-unreachable */
import { memo } from 'react';

function DashboardContent() {
	return (
		<>
			<img src="https://fotos-ecl.s3.amazonaws.com/Laredo+TX%2C+USA+(1).svg" alt="camino" />
		</>
	);
}

export default memo(DashboardContent);
